<?php

  // paste valid slack integration token
  const SLACK_TOKEN             = '';

  // specify if request user has to be validated against whitelist
  const CHECK_USER_ID_WHITELIST = false;

  // only specified users could perform request
  $user_id_whitelist            = [];

 ?>
